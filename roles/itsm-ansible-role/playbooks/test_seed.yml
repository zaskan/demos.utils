---
# Example: seed a running ITSM instance (see vars/test_seed.yml).
# Run from itsm-ansible-role/: ansible-playbook playbooks/test_seed.yml

- name: Test ITSM API seed role
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - vars/test_seed.yml
  roles:
    - role: itsm_api_seed
