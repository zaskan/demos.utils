Example:

```yaml
git:
  operation: # clone or push
  repository: # repository url
  directory: # local repository pah
  message: "commit message"
  email: user@email.com
  user: user
  git_key_path: # optional - force SSH
```