# SPDX-License-Identifier: GPL-2.0-or-later
# When running playbooks from this directory, the irc_stack role is two levels up
# (tests -> irc_stack -> roles) so ../.. is the "roles" root that contains irc_stack.
[defaults]
roles_path=../..
inventory=inventory
